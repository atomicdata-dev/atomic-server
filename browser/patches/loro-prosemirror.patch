--- a/src/sync-plugin.ts
+++ b/src/sync-plugin.ts
@@ -214,9 +214,8 @@
   if (anchor == null) {
     return;
   }
-  setTimeout(() => {
-    syncCursorsToPmSelection(view, anchor, focus);
-  });
+  // Restore before another input transaction can use the replaced selection.
+  syncCursorsToPmSelection(view, anchor, focus);
 }
 
 /**
--- a/dist/index.js
+++ b/dist/index.js
@@ -654,9 +654,8 @@
 	tr.setMeta(loroSyncPluginKey, { type: "non-local-updates" });
 	view.dispatch(tr);
 	if (anchor == null) return;
-	setTimeout(() => {
-		syncCursorsToPmSelection(view, anchor, focus);
-	});
+	// Restore before another input transaction can use the replaced selection.
+	syncCursorsToPmSelection(view, anchor, focus);
 }
 /**
 * Update ProseMirror selection based on the given Loro cursors.
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -655,9 +655,8 @@
 	tr.setMeta(loroSyncPluginKey, { type: "non-local-updates" });
 	view.dispatch(tr);
 	if (anchor == null) return;
-	setTimeout(() => {
-		syncCursorsToPmSelection(view, anchor, focus);
-	});
+	// Restore before another input transaction can use the replaced selection.
+	syncCursorsToPmSelection(view, anchor, focus);
 }
 /**
 * Update ProseMirror selection based on the given Loro cursors.
