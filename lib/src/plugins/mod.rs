//! The goal of this  module is to experiment with strategies for enabling plugins.
//! Each file inside of this module should represent a single extension to a Solid Server, which adds some functionality.
//! The exact needs and abstraction layer of a Plugin are not yet defined, so I think it's important to reflect upon the needed abstractions in each file

pub mod versioning;
