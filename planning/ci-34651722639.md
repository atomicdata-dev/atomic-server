# Develop CI failures (run 34651722639)

The merged develop commit `3c972984a` passed the separate-origin local run but
failed the embedded, same-origin Chromium run in CI. Track fixes against the
actual CI deployment shape; do not treat a preview-server pass as CI success.

- [x] Reproduce both managed recovery failures and managed-sync failure against
      the embedded `atomic.localhost` server; fix portal validation. Device-status
      interception now uses the existing Node-reachable URL helper.
- [x] Run calendar, quick-add, search, table insertion, template, and timer specs
      on the same-origin server: all 36 selected tests pass without retries.
      These remaining CI failures did not reproduce in that run.
- [ ] Finish checking flaky document editing, context-menu focus, row actions,
      quick-add, and concurrent timers under CPU slowdown.
- [x] Reproduce server-switch test failure: public atomicdata.dev/server returns
      404. Replace the external dependency with switching two real local drives
      by URL; three consecutive 4x CPU-slowed runs pass.
- [x] Unit regressions reproduce rejected loopback subdomains and Boolean read
      side effects. Boolean hooks no longer write false while rendering/loading.
- [ ] Validate the complete suite against the embedded same-origin server.
- [ ] Open a focused PR with remaining CI limitations stated explicitly.

CI diagnostics currently retain only a 20k-character log tail and the first 12
error-context files per shard, including retries. This loses later failures;
artifact retention should cover every failed test without logging resource payloads.
