The end-to-end tests are maintained by the `atomic-data-browser` repo and periodically put in this directory.

## How to run

- install playwright dependencies `npx playwright install`
- run your server `cargo run`
- `npm run test`
