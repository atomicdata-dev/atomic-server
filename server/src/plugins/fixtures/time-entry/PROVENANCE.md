# Neutral time-entry host fixture

Derived from the immutable Devonian fixture documented in the parent fixtures
README. Provider names, origins, secret slots, and identity prefixes use
reserved test values. Provider behavior is covered by external Devonian tests.
