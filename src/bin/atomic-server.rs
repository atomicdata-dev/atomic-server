fn main() {
  println!("Running Atomic Server on Some Port!")
  // Get Resource:
  // Check the
  // Get the resoure from the store
  // Return an HTML resource if the user asks for it
}

// Endpoints:
// Get Resource: individual resource fetch with content negotation (AD3, HTML,JSON, N-Triples)
// Home: HTML explain + OpenAPI spec something?, maybe an Atomic Navigation resource too?
// TPF:
// Path:
